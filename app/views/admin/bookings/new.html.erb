<div class="space-y-6">
  <div>
    <h1 class="text-2xl font-semibold text-slate-900 dark:text-white">Новая бронь</h1>
    <p class="text-sm text-slate-500 dark:text-slate-400 mt-1">Выберите гостя, даты — список свободных номеров обновится автоматически</p>
  </div>
  <%= render "form", booking: @booking %>
</div>
