<div class="space-y-6">
  <h1 class="text-2xl font-semibold text-slate-900 dark:text-white">Редактирование: <%= @room_category.name %></h1>
  <%= render "form", room_category: @room_category %>
</div>
