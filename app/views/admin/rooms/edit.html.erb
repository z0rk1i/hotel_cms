<div class="space-y-6">
  <div>
    <h1 class="text-2xl font-semibold text-slate-900 dark:text-white">Редактирование номера <%= @room.number %></h1>
    <p class="text-sm text-slate-500 dark:text-slate-400 mt-1">Категория: <%= @room.category.name %></p>
  </div>
  <%= render "form", room: @room %>
</div>
